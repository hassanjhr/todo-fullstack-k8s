apiVersion: dapr.io/v1alpha1
kind: Component
metadata:
  name: reminder-cron
spec:
  type: bindings.cron
  version: v1
  metadata:
    # Fires every 5 minutes — triggers POST /reminder-cron on the backend
    - name: schedule
      value: "@every 5m"
