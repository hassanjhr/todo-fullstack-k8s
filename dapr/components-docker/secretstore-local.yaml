apiVersion: dapr.io/v1alpha1
kind: Component
metadata:
  name: localsecrets
spec:
  type: secretstores.local.file
  version: v1
  metadata:
    # Mounted path inside the Dapr sidecar container
    - name: secretsFile
      value: "/secrets/secrets.json"
    - name: nestedSeparator
      value: ":"
