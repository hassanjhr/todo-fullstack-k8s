# Shared Configuration Template for Todo Full-Stack Web Application

# JWT Secret (CRITICAL: Must be identical in both frontend and backend)
# Generate with: openssl rand -hex 32
JWT_SECRET=your-secret-key-here-minimum-32-characters

# Database Configuration (Backend only)
DATABASE_URL=postgresql+asyncpg://user:password@ep-xxx.region.aws.neon.tech/dbname?sslmode=require

# API Configuration
API_URL=http://localhost:8000
FRONTEND_URL=http://localhost:3000

# Server Configuration
BACKEND_PORT=8000
FRONTEND_PORT=3000

# JWT Configuration
JWT_ALGORITHM=HS256
JWT_EXPIRATION_HOURS=24

# Environment
NODE_ENV=development
